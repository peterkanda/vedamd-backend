node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
.env-prod
.env.production
.env-*
*.log
npm-debug.log*
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# Bundle signing keys — generate with `npm run keys:generate` for local
# dev. The PRIVATE key is never committed; the public key lives in the
# bundle directory.
dev-keys/private-key.pem

# Python artifacts (from content tooling scripts)
__pycache__/
*.pyc
