[project]
name = "vedamd_cds"
authors = [
    { name = "VedaMD", email = "support@vedamd.io" }
]
description = "VedaMD clinical decision support for Frappe Health"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # frappe and requests are provided by bench.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
